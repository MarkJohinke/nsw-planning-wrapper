# Northern Beaches Dev Proxy

A lightweight proxy for Nominatim (OpenStreetMap geocoder) with a proper `User-Agent`.

## Endpoints

### `/geocode`
Convert an address into coordinates.

**Example:**
